'use strict';

/**
 * on-demand-sale controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::on-demand-sale.on-demand-sale');
